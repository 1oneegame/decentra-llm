# ML Push System - Система персонализированных рекомендаций банковских продуктов

## Описание

Комплексная система для анализа поведения клиентов банка и генерации персонализированных пуш-уведомлений с рекомендациями банковских продуктов. Система включает в себя ML модели для предсказания оптимальных продуктов, кластеризации клиентов и определения лучшего времени для отправки уведомлений.

## Возможности системы

- 🤖 **ML-предсказания** продуктов с высокой точностью
- 📊 **Кластеризация клиентов** по поведенческим паттернам
- ⏰ **Оптимизация времени** отправки уведомлений
- 📱 **Генерация персонализированных** пуш-сообщений
- 🔍 **Анализ транзакций** и переводов за 3 месяца
- 📈 **Расчет ожидаемой выгоды** для каждого продукта
- 🌐 **REST API** для интеграции с внешними системами

## Архитектура системы

### Основные компоненты:

1. **MLPushSystem** - основная система ML-предсказаний
2. **ClientAnalyzer** - анализ поведения клиентов
3. **FeatureEngineer** - инженерия признаков для ML
4. **PushGenerator** - генерация персонализированных сообщений
5. **FastAPI** - REST API для взаимодействия с системой

### ML модели:

- **Классификатор продуктов** - предсказывает оптимальный продукт
- **Регрессор выгоды** - рассчитывает ожидаемую выгоду
- **Кластеризация клиентов** - сегментация по поведению
- **Модель времени** - оптимальное время отправки

### Поддерживаемые продукты:

1. **Карта для путешествий** - кешбэк 4% на путешествия, отели, такси
2. **Премиальная карта** - базовый кешбэк 2-4% + премиум категории
3. **Кредитная карта** - кешбэк 10% на топ-3 категории + онлайн сервисы
4. **Депозит мультивалютный** - проценты 8% + бонус за валютные операции
5. **Депозит сберегательный** - максимальная ставка 12% (от 1М KZT)
6. **Депозит накопительный** - ставка 10% + бонус за пополнения
7. **Инвестиции** - экономия на комиссиях (от 50К KZT)
8. **Золотые слитки** - защитный актив

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Запуск API сервера

```bash
python run_server.py
```

API будет доступно по адресу: `http://localhost:8000`

### 3. Документация API

- **Swagger UI**: `http://localhost:8000/docs`
- **ReDoc**: `http://localhost:8000/redoc`
- **README API**: `API_README.md`

## Использование через API

### Предсказание для существующего клиента:

```bash
curl -X POST "http://localhost:8000/predict-push/1"
```

### Получение всех клиентов:

```bash
curl -X GET "http://localhost:8000/clients"
```

### Получение рекомендаций:

```bash
curl -X GET "http://localhost:8000/recommendations"
```

## Использование через Python

### Базовое использование:

```python
from src.ml_push_system import MLPushSystem

ml_system = MLPushSystem()
ml_system.load_and_prepare_data()
ml_system.train_ml_models()

# Предсказание для клиента
result = ml_system.predict_for_client(client_code=1)
print(f"Рекомендуемый продукт: {result['product']}")
print(f"Пуш-уведомление: {result['push_message']}")
```

## Структура данных

### Входные файлы:

- `data/raw/dataset/clients.csv` - профили клиентов
- `data/raw/dataset/client_{id}_transactions_3m.csv` - транзакции клиента за 3 месяца
- `data/raw/dataset/client_{id}_transfers_3m.csv` - переводы клиента за 3 месяца

### Выходные файлы:

- `data/processed/recommendations.csv` - готовые рекомендации для всех клиентов
- `models/ml_push_system.pkl` - сохраненные ML модели

## Алгоритм работы ML системы

1. **Загрузка и подготовка данных** - чтение профилей клиентов, транзакций и переводов
2. **Инженерия признаков** - создание признаков для ML моделей
3. **Обучение моделей** - классификатор продуктов, регрессор выгоды, кластеризация
4. **Анализ поведения клиента** - группировка трат по категориям, расчет паттернов
5. **ML-предсказание** - определение оптимального продукта и ожидаемой выгоды
6. **Кластеризация** - определение сегмента клиента
7. **Генерация пуш-сообщения** - создание персонализированного уведомления
8. **Оптимизация времени** - определение лучшего часа для отправки

## Метрики выгоды

- **Карта для путешествий**: 4% от трат на путешествия + 2% от валютных операций
- **Премиальная карта**: 2-4% от всех трат + 4% от премиум категорий + экономия на комиссиях
- **Кредитная карта**: 10% от топ-3 категорий + 10% от онлайн сервисов
- **Депозиты**: проценты от остатка + бонусы за активность
- **Инвестиции**: экономия на комиссиях от оборота

## API Эндпоинты

### Основные эндпоинты:

- `GET /` - проверка статуса API
- `POST /predict-push` - предсказание с загрузкой файлов
- `POST /predict-push/{client_code}` - предсказание для существующего клиента
- `GET /clients` - получение всех клиентов
- `GET /clients/{client_code}` - получение клиента по ID
- `GET /recommendations` - получение всех рекомендаций
- `GET /recommendations/{client_code}` - получение рекомендации по клиенту

### Примеры ответов:

```json
{
  "client_code": 1,
  "push_notification": "Айгерим, оптимизируйте ликвидность! Кредитное плечо для инвестиционных возможностей.",
  "recommended_product": "Кредит наличными",
  "confidence": 0.979,
  "optimal_time": 14
}
```

## Тестирование

### Запуск тестов:

```bash
python test_push_prediction.py    # Тест предсказаний
python test_api.py               # Тест API эндпоинтов
```

### Тестирование через Postman:

1. Импортируйте коллекцию из `API_README.md`
2. Запустите сервер: `python run_server.py`
3. Тестируйте эндпоинты по адресу `http://localhost:8000`

## Производительность

- **Время обучения**: ~2-3 минуты (первый запуск)
- **Время предсказания**: ~100-200ms на клиента
- **Точность модели**: 85-95% для классификации продуктов
- **Поддерживаемые клиенты**: до 10,000 одновременно

## Развертывание

### Локальная разработка:

```bash
python run_server.py
```

### Продакшн (с Docker):

```bash
docker build -t ml-push-system .
docker run -p 8000:8000 ml-push-system
```

## Поддержка

- 📖 **Документация API**: `API_README.md`
- 🔧 **Swagger UI**: `http://localhost:8000/docs`
- 📊 **ReDoc**: `http://localhost:8000/redoc`
- 🐛 **Логи**: выводятся в консоль при запуске
